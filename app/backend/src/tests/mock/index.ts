import Users from './users.mock';
import Token from './token.mock';
import { Login, WrongEmail, WrongPassword, MissingPass, MissingEmail, } from './login.mock';
import Teams from './teams.mock';
import Matches, { CreatedMatch, Finished, NewMatch, NoMatch,
  SameMatch, Unfinished, WrongMatch } from './matches.mock';

export {
  Users,
  Login,
  WrongEmail,
  WrongPassword,
  MissingPass,
  MissingEmail,
  Token,
  Teams,
  Matches,
  Unfinished,
  Finished,
  NewMatch,
  CreatedMatch,
  WrongMatch,
  SameMatch,
  NoMatch,
}
